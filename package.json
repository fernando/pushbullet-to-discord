{
  "name": "pushbullet-to-discord",
  "version": "0.0.1",
  "description": "",
  "main": "build/src/app.js",
  "types": "build/src/app.d.ts",
  "files": [
    "build/src"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check",
    "start": "tsc -p . && node ./build/src/app.js"
  },
  "devDependencies": {
    "gts": "^1.0.0",
    "typescript": "~3.4.0"
  },
  "dependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/node": "^12.0.10",
    "@types/request": "^2.48.1",
    "@types/request-promise-native": "^1.0.16",
    "@types/ws": "^6.0.1",
    "dotenv": "^8.0.0",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.7",
    "ws": "^7.4.6"
  }
}
